import React from "react";
import styled from "styled-components";
const HeaderStyle = styled.h1`
  color: black;
  font-weight: bolder;
  margin: 1rem;
`;

const Header = () => <HeaderStyle>Quiz</HeaderStyle>;

export default Header;
